//NOTE: Only 3 functions in stack [push(), pop(), top()], & LIFO order.

#include <bits/stdc++.h>
#include <stack>                            //Library
using namespace std;

int main(){
    stack<int> s;         //initilization
    s.push(4);
    s.pop();
    s.top();
    return 0;
}